tunnel: REPLACE_WITH_TUNNEL_ID
credentials-file: /etc/cloudflared/REPLACE_WITH_TUNNEL_ID.json

ingress:
  - hostname: mcp.clideck.com
    service: http://127.0.0.1:8787
    originRequest:
      connectTimeout: 10s
      httpHostHeader: mcp.clideck.com
  - service: http_status:404
